The full report is printed to stdout above. Headline finding: **38 broken inline image refs on the live site** — `projects/*.md` use `../assets/images/...` (resolves to root `/assets/images/...` → 404) when images actually live at `/projects/assets/images/<project>/...` (200). Affects 5 projects' pages across EN+UK; hero images render correctly because `_data/projects.yml` uses absolute paths. This is a live visible bug worth a small `fix-projects-inline-image-refs` PR.

Also flagged two stale docs in CLAUDE.md: nav is in `_data/nav.yml` (not `_config.yml`'s `header_pages`), and listings are data-driven from `_data/projects.yml` / `_data/portfolio.yml` rather than file-presence — a non-obvious gotcha when adding new entries.
