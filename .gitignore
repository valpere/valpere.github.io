# Jekyll directories — override global _* gitignore pattern
!_data/
!_includes/
!_layouts/
!_sass/
!_posts/
!_drafts/
!_config.yml

# GitHub Actions — override global .* gitignore pattern
!.github/

# OS generated files
.DS_Store
.DS_Store?

._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Dependencies
node_modules/

# Build files
dist/
build/

# Jekyll files
_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor

tmp/

# Skill telemetry (local only)
.claude/skills/fix-review/telemetry.jsonl

# Session recall — per-project, local-only context log/index
.claude/session-log.md
.claude/sessions.db

# Claude Code local-only settings — never commit (per-machine hook wiring)
.claude/settings.local.json
